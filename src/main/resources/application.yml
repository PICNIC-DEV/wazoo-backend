spring:
  application:
    name: api.wazoo.com

  datasource:
    url: jdbc:mysql://localhost:3306/loginTest
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: db650571!5588

  jpa:
    hibernate:
      ddl-auto: update
      properties:
        hibernate:
          dialect: org.hibernate.dialect.MySQLDialect
    show-sql: true

